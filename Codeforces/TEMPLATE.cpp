#include <bits/stdc++.h>

using i64 = long long;

using namespace std;

void foo() {

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    // int T = 1;
    int T;
    cin >> T;

    for (i64 i = 0; i < T; i++) foo();

    return 0;
}